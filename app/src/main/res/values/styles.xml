<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="AppBaseTheme">
        <!-- 设置无标题-->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置全屏-->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- <item name="colorPrimary">@color/colorPrimary</item>
         <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
         <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="m_home_radio_style">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">140dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center</item>
        <item name="com.wisesoft.traveltv:gTextPadding">10dp</item>
        <item name="com.wisesoft.traveltv:gTextColor">@color/text_color_gray</item>
        <item name="com.wisesoft.traveltv:gTextSize">@dimen/text_normal_size</item>
        <item name="com.wisesoft.traveltv:gIconWidth">50dp</item>
        <item name="com.wisesoft.traveltv:gIconHeight">50dp</item>
    </style>

    <style name="head_style">
        <item name="android:layout_height">@dimen/head_height</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginBottom">24dp</item>
    </style>

    <style name="RatingStyle" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/layer_live_rating_bar</item>
        <!-- 根据自定义星星图片的大小,设置相应的值,否则可能显示不全 -->
        <item name="android:numColumns">5</item>
        //这里放一些你觉得公共的属性(你可以在控件里覆盖这里的属性)
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="dialog_no_border" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item><!-- 边框 -->
        <item name="android:windowIsFloating">true</item><!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item><!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>  <!-- 无标题 -->
       <!-- <item name="android:windowBackground">#00000000</item> 背景图片 -->
        <item name="android:backgroundDimEnabled">true</item>  <!-- 模糊 -->
        <!-- 对话框弹出后，父窗体是否为全黑，需要跟backgroundDimEnabled配合使用 -->
        <item name="android:backgroundDimAmount">0.4</item>
    </style>
</resources>
